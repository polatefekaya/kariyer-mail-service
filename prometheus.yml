global:
  scrape_interval: 15s # How often to pull metrics from .NET
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'kariyer-mail-api'
    static_configs:
      - targets: ['host.docker.internal:5000']
